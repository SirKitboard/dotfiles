[user]
	name  = YOUR_NAME
	email = YOUR_EMAIL

[init]
	defaultBranch = main

[core]
	pager = cat

[merge]
	ff = false

[pull]
	ff = only

[filter "lfs"]
	clean    = git-lfs clean -- %f
	smudge   = git-lfs smudge -- %f
	process  = git-lfs filter-process
	required = true
